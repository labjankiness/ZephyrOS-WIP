[options]
Architecture = auto
CheckSpace
SigLevel = Required DatabaseOptional
LocalFileSigLevel = Optional

Include = /etc/pacman.d/mirrorlist

# ZephyrOS local repo (AUR packages built into build/localrepo/)
# This is generated by build/scripts/build-localrepo.sh
Include = airootfs/etc/pacman.d/local.conf

[core]
Include = /etc/pacman.d/mirrorlist

[extra]
Include = /etc/pacman.d/mirrorlist

[community]
Include = /etc/pacman.d/mirrorlist

